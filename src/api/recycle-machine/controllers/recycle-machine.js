'use strict';

/**
 * recycle-machine controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::recycle-machine.recycle-machine');
