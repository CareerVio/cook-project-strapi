'use strict';

/**
 * donation-organization service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::donation-organization.donation-organization');
